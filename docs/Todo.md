## TODO

P0 / P1 正确性重构（检索距离、摄取结果、按市场分析师、A 股 DataProviders、交易口径、MCP 白名单等）见 [P0-P1重构实施计划.md](./P0-P1重构实施计划.md)。下文仅保留该计划范围之外、仍需深化的事项。

### Agent 与 Prompt

- `xml prompt` 方案评估与落地。
- `SK to Agent Framework` 的进一步迁移与能力收口。
- 精细化任务指导：让主 Agent 输出更明确的目标、边界和结果格式。
- 将阶段性工作总结沉淀到外部记忆，并以轻量上下文继续后续任务。

### MCP 工具约束

- 继续增强 MCP 工具的分组与最小暴露策略，避免模型在多服务器场景下无约束试探。
- 评估按服务器或业务域拆分插件命名空间，例如 `mcp.<serverName>` 或 `mcp.search.<serverName>`。
- 继续完善函数调用范围限制与提示词级工具使用策略。

### RAG 与文档链路

- 继续收敛文档摄取链，避免 `PDF/DOCX -> Markdown -> 再解析 Markdown` 的双重解析路径持续扩散。
- 评估是否需要更稳定的文档块抽取方案，而不是继续堆叠格式转换启发式。

### 多 Agent 协作

- 与其让子 Agent 通过对话历史传递大块结构化结果，不如优先写入文件系统并传回轻量引用，进一步降低失真与 Token 成本。