<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="200"
             x:Class="MarketAssistant.Views.Components.ProgressDisplayView"
             x:Name="Root">

    <UserControl.Styles>
        <!-- 脉动动画样式 -->
        <Style Selector="Ellipse.Pulse">
            <Style.Animations>
                <Animation Duration="0:0:1.5" IterationCount="Infinite">
                    <KeyFrame Cue="0%">
                        <Setter Property="Opacity" Value="0.3"/>
                    </KeyFrame>
                    <KeyFrame Cue="50%">
                        <Setter Property="Opacity" Value="0.8"/>
                    </KeyFrame>
                    <KeyFrame Cue="100%">
                        <Setter Property="Opacity" Value="0.3"/>
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
    </UserControl.Styles>
    
    <Border Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="{StaticResource DefaultBorderThickness}"
            CornerRadius="{StaticResource DefaultCornerRadius}"
            Padding="{StaticResource ProgressDialogPadding}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Width="{StaticResource ProgressDialogWidth}">
        
        <StackPanel Spacing="{StaticResource DefaultIconSize}">
            
            <!-- 标题 -->
            <TextBlock Text="🤖 AI 分析中..."
                       FontSize="{StaticResource LargeFontSize}"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource PrimaryBrush}"/>

            <!-- 进度指示器区域 -->
            <StackPanel Spacing="{StaticResource LargeSpacing}" HorizontalAlignment="Center">
                
                <!-- 圆形脉动指示器 -->
                <Grid Width="{StaticResource ProgressCircleSize}" 
                      Height="{StaticResource ProgressCircleSize}"
                      HorizontalAlignment="Center">
                    
                    <!-- 外圆 - 脉动效果 -->
                    <Ellipse Width="{StaticResource ProgressCircleSize}" 
                             Height="{StaticResource ProgressCircleSize}"
                             Fill="{DynamicResource PrimaryBrush}"
                             Opacity="0.1"
                             Classes="Pulse"/>
                    
                    <!-- 中圆 - 进度环 -->
                    <Ellipse Width="{StaticResource ProgressCircleInnerSize}" 
                             Height="{StaticResource ProgressCircleInnerSize}"
                             Stroke="{DynamicResource BorderBrush}"
                             StrokeThickness="{StaticResource TinySpacing}"
                             Fill="Transparent"/>
                    
                    <!-- 进度百分比显示 -->
                    <TextBlock x:Name="ProgressPercentage"
                               Text="0%"
                               FontSize="{StaticResource TitleFontSize}"
                               FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Grid>

                <!-- 水平进度条 -->
                <ProgressBar x:Name="ProgressBar"
                             Height="{StaticResource TinySpacing}"
                             Width="{StaticResource ProgressBarWidth}"
                             Minimum="0"
                             Maximum="100"
                             Value="0"
                             CornerRadius="{StaticResource TinyCornerRadius}"
                             Foreground="{DynamicResource PrimaryBrush}"
                             Background="{DynamicResource BorderBrush}"
                             HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- 阶段说明 -->
            <StackPanel Spacing="{StaticResource SmallSpacing}">
                <TextBlock x:Name="StageLabel"
                           Text="正在准备分析..."
                           FontSize="{StaticResource LabelFontSize}"
                           FontWeight="Medium"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                
                <TextBlock x:Name="DetailLabel"
                           Text="即将开始智能分析"
                           FontSize="{StaticResource SmallFontSize}"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           MaxWidth="{StaticResource ProgressDetailMaxWidth}"/>
            </StackPanel>

            <!-- 提示信息 -->
            <Border Background="{DynamicResource PageBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="{StaticResource DefaultBorderThickness}"
                    CornerRadius="{StaticResource CardCornerRadius}"
                    Padding="{StaticResource DefaultCardPadding}">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            Spacing="{StaticResource SmallSpacing}">
                    <TextBlock Text="💡"
                               FontSize="{StaticResource LabelFontSize}"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="分析过程可能需要 1-3 分钟，请耐心等待"
                               FontSize="{StaticResource SmallFontSize}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
