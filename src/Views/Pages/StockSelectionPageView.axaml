<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:MarketAssistant.ViewModels"
             xmlns:controls="using:MarketAssistant.Views.Controls"
             xmlns:models="using:MarketAssistant.Applications.StockSelection.Models"
             xmlns:converts="using:MarketAssistant.Converts"
             mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="800"
             x:Class="MarketAssistant.Views.Pages.StockSelectionPageView"
             x:DataType="vm:StockSelectionPageViewModel"
             x:Name="Root">

    <Design.DataContext>
        <vm:StockSelectionPageViewModel/>
    </Design.DataContext>

    <UserControl.Resources>
        <converts:NullableValueConverter x:Key="NullableValueConverter"/>
        <converts:NullableVisibilityConverter x:Key="NullableVisibilityConverter"/>
    </UserControl.Resources>

    <Grid RowDefinitions="Auto,*">
        <!-- 加载指示器（优化后的覆盖层） -->
        <Border Grid.Row="0" Grid.RowSpan="2"
                Background="{StaticResource OverlayBrush}"
                IsVisible="{Binding IsBusy}"
                ZIndex="1000">
            <Border Background="{DynamicResource CardBackgroundBrush}"
                    CornerRadius="{StaticResource LargeCornerRadius}"
                    Padding="{StaticResource ProgressDialogPadding}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="{StaticResource InputAreaMinWidth}"
                    MaxWidth="{StaticResource InputAreaMaxWidth}"
                    BoxShadow="{StaticResource ElevatedBoxShadow}">
                <StackPanel Spacing="{StaticResource LargeSpacing}">
                    <!-- 加载动画区域 - 三点脉冲动画 -->
                    <StackPanel Orientation="Horizontal" 
                                Spacing="{StaticResource SmallSpacing}"
                                HorizontalAlignment="Center">
                        <Ellipse Width="{StaticResource MicroIconSize}" Height="{StaticResource MicroIconSize}" 
                                 Fill="{DynamicResource SystemAccentColor}">
                            <Ellipse.Styles>
                                <Style Selector="Ellipse">
                                    <Style.Animations>
                                        <Animation Duration="0:0:1.4" IterationCount="Infinite">
                                            <KeyFrame Cue="0%">
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </KeyFrame>
                                            <KeyFrame Cue="20%">
                                                <Setter Property="Opacity" Value="1.0"/>
                                            </KeyFrame>
                                            <KeyFrame Cue="40%">
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </KeyFrame>
                                        </Animation>
                                    </Style.Animations>
                                </Style>
                            </Ellipse.Styles>
                        </Ellipse>
                        
                        <Ellipse Width="{StaticResource MicroIconSize}" Height="{StaticResource MicroIconSize}" 
                                 Fill="{DynamicResource SystemAccentColor}">
                            <Ellipse.Styles>
                                <Style Selector="Ellipse">
                                    <Style.Animations>
                                        <Animation Duration="0:0:1.4" IterationCount="Infinite" Delay="0:0:0.2">
                                            <KeyFrame Cue="0%">
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </KeyFrame>
                                            <KeyFrame Cue="20%">
                                                <Setter Property="Opacity" Value="1.0"/>
                                            </KeyFrame>
                                            <KeyFrame Cue="40%">
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </KeyFrame>
                                        </Animation>
                                    </Style.Animations>
                                </Style>
                            </Ellipse.Styles>
                        </Ellipse>
                        
                        <Ellipse Width="{StaticResource MicroIconSize}" Height="{StaticResource MicroIconSize}" 
                                 Fill="{DynamicResource SystemAccentColor}">
                            <Ellipse.Styles>
                                <Style Selector="Ellipse">
                                    <Style.Animations>
                                        <Animation Duration="0:0:1.4" IterationCount="Infinite" Delay="0:0:0.4">
                                            <KeyFrame Cue="0%">
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </KeyFrame>
                                            <KeyFrame Cue="20%">
                                                <Setter Property="Opacity" Value="1.0"/>
                                            </KeyFrame>
                                            <KeyFrame Cue="40%">
                                                <Setter Property="Opacity" Value="0.3"/>
                                            </KeyFrame>
                                        </Animation>
                                    </Style.Animations>
                                </Style>
                            </Ellipse.Styles>
                        </Ellipse>
                    </StackPanel>
                    
                    <!-- 文本信息 -->
                    <StackPanel Spacing="{StaticResource SmallSpacing}">
                        <TextBlock Text="正在分析中" 
                                   FontSize="{StaticResource LargeFontSize}"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="AI 正在为您精选优质股票，请稍候..."
                                   FontSize="{StaticResource LabelFontSize}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   MaxWidth="{StaticResource MessageMaxWidth}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="{StaticResource PagePadding}" Spacing="{StaticResource LargeSpacing}">
                
                <!-- 页面标题 -->
                <TextBlock Text="AI智能选股" 
                           FontSize="{StaticResource TitleFontSize}"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="{StaticResource TinyBottomMargin}"/>

            <!-- 智能选股输入区域 -->
            <controls:CardView>
                <StackPanel Spacing="{StaticResource DefaultSpacing}">
                    <!-- 选股模式选择器 -->
                    <TextBlock Text="选股模式" 
                               FontSize="{StaticResource SectionTitleFontSize}" 
                               FontWeight="Bold"/>
                    
                    <!-- 验证提示消息 -->
                    <Border Background="{StaticResource WarningLight}"
                            BorderBrush="{StaticResource WarningBorder}"
                            BorderThickness="{StaticResource DefaultBorderThickness}"
                            CornerRadius="{StaticResource CardCornerRadius}"
                            Padding="{StaticResource DefaultCardPadding}"
                            IsVisible="{Binding HasValidationMessage}">
                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource SmallSpacing}">
                            <TextBlock Text="ℹ️" 
                                       FontSize="{StaticResource SectionTitleFontSize}"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding ValidationMessage}"
                                       FontSize="{StaticResource LabelFontSize}"
                                       Foreground="{StaticResource WarningText}"
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <ItemsControl ItemsSource="{Binding SelectionModes}" Height="{StaticResource ModeSelectionHeight}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="{StaticResource SmallSpacing}"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:SelectionModeItem">
								<Border Classes.mode-card="True"
                                        Classes.selected="{Binding IsSelected}"
                                        MinWidth="{StaticResource ButtonMinWidth}">
                                    <StackPanel Spacing="{StaticResource TinySpacing}">
                                        <TextBlock Text="{Binding Icon}" 
                                                   FontSize="{StaticResource LargeFontSize}" 
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}" 
                                                   FontSize="{StaticResource SmallFontSize}" 
                                                   FontWeight="Bold"
                                                   Classes="mode-name"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <!-- 统一输入框 -->
                    <TextBox Text="{Binding CurrentInputContent}"
                             Watermark="{Binding CurrentPlaceholder}"
                             Height="{StaticResource CompactTextAreaHeight}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             IsVisible="{Binding IsInputAreaVisible}"/>
                    
                    <!-- 操作按钮 -->
                    <StackPanel Orientation="Horizontal" 
                                Spacing="{StaticResource SmallSpacing}"
                                IsVisible="{Binding IsInputAreaVisible}">
                        <Button Content="{Binding CurrentButtonText}"
                                Command="{Binding ExecuteAnalysisCommand}"
                                IsEnabled="{Binding !IsBusy}"
                                Background="{DynamicResource SystemAccentColor}"
                                Foreground="White"
                                HorizontalAlignment="Stretch"
                                Padding="{StaticResource PagePadding}"
                                MinWidth="{StaticResource ButtonMinWidth}"/>
                        <Button Content="清除"
								Command="{Binding ClearContentCommand}"
                                Padding="{StaticResource PagePadding}"
                                MinWidth="{StaticResource SmallButtonMinWidth}"/>
                    </StackPanel>
                </StackPanel>
            </controls:CardView>

            <!-- 快速选股策略 -->
            <controls:CardView IsVisible="{Binding IsQuickStrategyAreaVisible}">
                <StackPanel Spacing="{StaticResource DefaultSpacing}">
                    <TextBlock Text="快速选股策略" 
                               FontSize="{StaticResource SectionTitleFontSize}" 
                               FontWeight="SemiBold"/>
                    <TextBlock Text="选择预设的选股策略，快速获得投资建议" 
                               FontSize="{StaticResource SmallFontSize}" 
                               Foreground="{DynamicResource TextSecondaryBrush}"/>

                    <ItemsControl ItemsSource="{Binding QuickStrategies}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="models:QuickSelectionStrategyInfo">
                                <Border Classes="strategy-card" Margin="{StaticResource SmallMargin}">
                                    <Grid ColumnDefinitions="Auto, *">
                                        <!-- 图标 -->
                                        <Border Grid.Column="0"
                                                Width="{StaticResource LargeIconSize}" 
                                                Height="{StaticResource LargeIconSize}"
                                                CornerRadius="{StaticResource SmallCornerRadius}"
                                                Background="{DynamicResource SurfaceVariantBrush}"
                                                Margin="{StaticResource RightMargin}"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Icon}"
                                                       FontSize="{StaticResource TitleFontSize}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- 文本内容 -->
                                        <StackPanel Grid.Column="1" Spacing="{StaticResource TinySpacing}" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" 
                                                       FontSize="{StaticResource LabelFontSize}" 
                                                       FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Description}" 
                                                       FontSize="{StaticResource SmallFontSize}" 
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </controls:CardView>
            
            <!-- 选股结果显示区域 -->
            <controls:CardView IsVisible="{Binding HasResult}">
                <StackPanel Spacing="{StaticResource LargeSpacing}">
                    <TextBlock Text="选股结果" 
                               FontSize="{StaticResource SubTitleFontSize}" 
                               FontWeight="Bold"/>
                    
                    <!-- AI 置信度显示 -->
                    <Border Background="{StaticResource InfoBackgroundAlt}"
                            BorderBrush="{StaticResource InfoBorder}"
                            BorderThickness="{StaticResource ThickBorderThickness}"
                            CornerRadius="{StaticResource CardCornerRadius}"
                            Padding="{StaticResource CardPadding}">
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <StackPanel Grid.Column="0" 
                                        Orientation="Horizontal" 
                                        Spacing="{StaticResource SmallSpacing}"
                                        VerticalAlignment="Center">
                                <TextBlock Text="🤖" 
                                           FontSize="{StaticResource LargeFontSize}"/>
                                <TextBlock Text="AI 置信度" 
                                           FontSize="{StaticResource MediumFontSize}" 
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <ProgressBar Grid.Column="1"
                                         Value="{Binding SelectionResult.ConfidenceScore}"
                                         Maximum="100"
                                         Height="{StaticResource ProgressBarHeight}"
                                         Margin="{StaticResource HorizontalMargin}"
                                         CornerRadius="{StaticResource TinyCornerRadius}"
                                         VerticalAlignment="Center"/>
                            
                            <TextBlock Grid.Column="2"
                                       Text="{Binding SelectionResult.ConfidenceScore, StringFormat={}{0:F1}%}"
                                       FontSize="{StaticResource LargeFontSize}"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource InfoBorder}"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    
                    <!-- 分析摘要 -->
                    <Border Background="{DynamicResource ItemBackgroundBrush}"
                            CornerRadius="{StaticResource CardCornerRadius}"
                            Padding="{StaticResource CardPadding}">
                        <StackPanel Spacing="{StaticResource SmallSpacing}">
                            <TextBlock Text="📋 分析摘要" 
                                       FontSize="{StaticResource MediumFontSize}" 
                                       FontWeight="Bold"/>
                            <TextBlock Text="{Binding SelectionResult.AnalysisSummary}"
                                       FontSize="{StaticResource LabelFontSize}"
                                       TextWrapping="Wrap"
                                       LineHeight="{StaticResource DefaultLineHeight}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 市场环境分析 -->
                    <Border Background="{DynamicResource ItemBackgroundBrush}"
                            CornerRadius="{StaticResource CardCornerRadius}"
                            Padding="{StaticResource CardPadding}">
                        <StackPanel Spacing="{StaticResource SmallSpacing}">
                            <TextBlock Text="📊 市场环境分析" 
                                       FontSize="{StaticResource MediumFontSize}" 
                                       FontWeight="Bold"/>
                            <TextBlock Text="{Binding SelectionResult.MarketEnvironmentAnalysis}"
                                       FontSize="{StaticResource LabelFontSize}"
                                       TextWrapping="Wrap"
                                       LineHeight="{StaticResource DefaultLineHeight}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 投资建议 -->
                    <Border Background="{StaticResource InfoLight}"
                            BorderBrush="{StaticResource InfoBorder}"
                            BorderThickness="{StaticResource ThickBorderThickness}"
                            CornerRadius="{StaticResource CardCornerRadius}"
                            Padding="{StaticResource CardPadding}">
                        <StackPanel Spacing="{StaticResource SmallSpacing}">
                            <TextBlock Text="💡 投资建议" 
                                       FontSize="{StaticResource MediumFontSize}" 
                                       FontWeight="Bold"
                                       Foreground="{StaticResource InfoText}"/>
                            <TextBlock Text="{Binding SelectionResult.InvestmentAdvice}"
                                       FontSize="{StaticResource LabelFontSize}"
                                       Foreground="{StaticResource InfoTextDark}"
                                       TextWrapping="Wrap"
                                       LineHeight="{StaticResource DefaultLineHeight}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 推荐股票列表 -->
                    <TextBlock Text="📈 推荐股票" 
                               FontSize="{StaticResource SectionTitleFontSize}" 
                               FontWeight="Bold" 
                               Margin="{StaticResource SmallTopMargin}"/>
                    
                    <ItemsControl ItemsSource="{Binding RecommendedStocks}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Spacing="{StaticResource DefaultSpacing}"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="models:StockRecommendation">
                                    <Border Background="{DynamicResource ItemBackgroundBrush}"
                                            CornerRadius="{StaticResource MediumCornerRadius}"
                                            Padding="{StaticResource CardPadding}"
                                            BoxShadow="{StaticResource CardBoxShadow}">
                                        <StackPanel Spacing="{StaticResource DefaultSpacing}">
                                            
                                            <!-- 第一部分：股票基本信息 + 推荐评分 -->
                                            <Grid ColumnDefinitions="*,Auto">
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Symbol}" 
                                                               FontSize="{StaticResource SubTitleFontSize}" 
                                                               FontWeight="Bold"
                                                               Foreground="{DynamicResource SystemAccentColor}"/>
                                                    <TextBlock Text="{Binding Name}" 
                                                               FontSize="{StaticResource BodyFontSize}"
                                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                                               Margin="{StaticResource TinyTopMargin}"/>
                                                </StackPanel>
                                    
                                                <!-- 推荐评分高亮 -->
                                                <Border Grid.Column="1"
                                                        Background="{DynamicResource SystemAccentColor}"
                                                        CornerRadius="{StaticResource CardCornerRadius}"
                                                        Padding="{StaticResource DefaultCardPadding}">
                                                    <StackPanel Spacing="{StaticResource CompactSpacing}">
                                                        <TextBlock Text="{Binding RecommendationScore, StringFormat={}{0}分}"
                                                                   FontSize="{StaticResource SubTitleFontSize}"
                                                                   FontWeight="Bold"
                                                                   Foreground="{StaticResource WarningBorder}"
                                                                   HorizontalAlignment="Center"/>
                                                        <TextBlock Text="推荐评分"
                                                                   FontSize="{StaticResource MiniFontSize}"
                                                                   Foreground="White"
                                                                   HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                            
                                            <!-- 第二部分：核心投资指标（始终显示，保持布局一致）-->
                                            <Grid ColumnDefinitions="*,*,*">
                                                <!-- 目标价格 -->
                                                <Border Grid.Column="0"
                                                        Background="{StaticResource SuccessLight}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"
                                                        Padding="{StaticResource CompactCardPadding}"
                                                        Margin="{StaticResource TinyRightMargin}">
                                                    <StackPanel Spacing="{StaticResource TinySpacing}">
                                                        <TextBlock Text="🎯 目标价"
                                                                   FontSize="{StaticResource TinyFontSize}"
                                                                   Foreground="{StaticResource SuccessText}"
                                                                   FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding TargetPrice, Converter={StaticResource NullableValueConverter}, ConverterParameter={}{0:F2}元|待定}"
                                                                   FontSize="{StaticResource MediumFontSize}"
                                                                   FontWeight="Bold"
                                                                   Foreground="{StaticResource SuccessAccent}"/>
                                                    </StackPanel>
                                                </Border>
                                                
                                                <!-- 预期收益率 -->
                                                <Border Grid.Column="1"
                                                        Background="{StaticResource InfoBackground}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"
                                                        Padding="{StaticResource CompactCardPadding}"
                                                        Margin="{StaticResource TinyHorizontalMargin}">
                                                    <StackPanel Spacing="{StaticResource TinySpacing}">
                                                        <TextBlock Text="📈 预期收益"
                                                                   FontSize="{StaticResource TinyFontSize}"
                                                                   Foreground="{StaticResource InfoText}"
                                                                   FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding ExpectedReturn, Converter={StaticResource NullableValueConverter}, ConverterParameter=+{0:F1}%|待定}"
                                                                   FontSize="{StaticResource MediumFontSize}"
                                                                   FontWeight="Bold"
                                                                   Foreground="{StaticResource InfoAccent}"/>
                                                    </StackPanel>
                                                </Border>
                                                
                                                <!-- 止损价格 -->
                                                <Border Grid.Column="2"
                                                        Background="{StaticResource DangerBackground}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"
                                                        Padding="{StaticResource CompactCardPadding}"
                                                        Margin="{StaticResource TinyRightMargin}">
                                                    <StackPanel Spacing="{StaticResource TinySpacing}">
                                                        <TextBlock Text="🛑 止损价"
                                                                   FontSize="{StaticResource TinyFontSize}"
                                                                   Foreground="{StaticResource DangerText}"
                                                                   FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding StopLoss, Converter={StaticResource NullableValueConverter}, ConverterParameter={}{0:F2}元|待定}"
                                                                   FontSize="{StaticResource MediumFontSize}"
                                                                   FontWeight="Bold"
                                                                   Foreground="{StaticResource DangerAccent}"/>
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                            
                                            <!-- 第三部分：核心指标与推荐理由整合 -->
                                            <Grid ColumnDefinitions="Auto, Auto, Auto, *" Margin="0,4,0,0">
                                                <!-- 建议仓位（仅有值时显示）-->
                                                <Border Grid.Column="0"
                                                        Margin="0,0,8,0"
                                                        Background="{StaticResource WarningLight}"
                                                        BorderBrush="{StaticResource WarningBorder}"
                                                        BorderThickness="{StaticResource DefaultBorderThickness}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"
                                                        Padding="{StaticResource DefaultCardPadding}"
                                                        VerticalAlignment="Stretch"
                                                        IsVisible="{Binding RecommendedPosition, Converter={StaticResource NullableVisibilityConverter}}">
                                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource SmallSpacing}" VerticalAlignment="Center">
                                                        <TextBlock Text="💰"
                                                                   FontSize="{StaticResource LabelFontSize}"
                                                                   VerticalAlignment="Center"/>
                                                        <StackPanel Spacing="{StaticResource CompactSpacing}">
                                                            <TextBlock Text="建议仓位"
                                                                       FontSize="{StaticResource MiniFontSize}"
                                                                       Foreground="{StaticResource WarningText}"/>
                                                            <TextBlock Text="{Binding RecommendedPosition, StringFormat={}{0:F0}%}"
                                                                       FontSize="{StaticResource LabelFontSize}"
                                                                       FontWeight="Bold"
                                                                       Foreground="{StaticResource WarningText}"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>
                                                
                                                <!-- 持有期限（仅有值时显示）-->
                                                <Border Grid.Column="1"
                                                        Margin="0,0,8,0"
                                                        Background="{StaticResource InfoLight}"
                                                        BorderBrush="{StaticResource InfoBorder}"
                                                        BorderThickness="{StaticResource DefaultBorderThickness}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"
                                                        Padding="{StaticResource DefaultCardPadding}"
                                                        VerticalAlignment="Stretch"
                                                        IsVisible="{Binding RecommendedHoldingPeriod, Converter={StaticResource NullableVisibilityConverter}}">
                                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource SmallSpacing}" VerticalAlignment="Center">
                                                        <TextBlock Text="⏱️"
                                                                   FontSize="{StaticResource LabelFontSize}"
                                                                   VerticalAlignment="Center"/>
                                                        <StackPanel Spacing="{StaticResource CompactSpacing}">
                                                            <TextBlock Text="持有期限"
                                                                       FontSize="{StaticResource MiniFontSize}"
                                                                       Foreground="{StaticResource InfoText}"/>
                                                            <TextBlock Text="{Binding RecommendedHoldingPeriod, StringFormat={}{0}天}"
                                                                       FontSize="{StaticResource LabelFontSize}"
                                                                       FontWeight="Bold"
                                                                       Foreground="{StaticResource InfoText}"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>

                                                <!-- 风险等级（始终显示）-->
                                                <Border Grid.Column="2" 
                                                        Margin="0,0,8,0"
                                                        Background="{StaticResource DangerLight}"
                                                        BorderBrush="{StaticResource DangerBorder}"
                                                        BorderThickness="{StaticResource ThickBorderThickness}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"
                                                        Padding="{StaticResource DefaultCardPadding}"
                                                        VerticalAlignment="Stretch">
                                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource SmallSpacing}" VerticalAlignment="Center">
                                                        <TextBlock Text="⚠️"
                                                                   FontSize="{StaticResource LabelFontSize}"
                                                                   VerticalAlignment="Center"/>
                                                        <StackPanel Spacing="{StaticResource CompactSpacing}">
                                                            <TextBlock Text="风险等级"
                                                                       FontSize="{StaticResource MiniFontSize}"
                                                                       Foreground="{StaticResource DangerText}"/>
                                                            <TextBlock Text="{Binding RiskLevel}"
                                                                       FontSize="{StaticResource LabelFontSize}"
                                                                       FontWeight="Bold"
                                                                       Foreground="{StaticResource DangerText}"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>

                                                <!-- 推荐理由卡片 -->
                                                <Border Grid.Column="3"
                                                        Background="{StaticResource WarningBackground}"
                                                        BorderBrush="{StaticResource WarningBorder}"
                                                        BorderThickness="{StaticResource DefaultBorderThickness}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"
                                                        Padding="{StaticResource DefaultCardPadding}">
                                                    <StackPanel Spacing="{StaticResource SmallSpacing}">
                                                        <TextBlock Text="💡 推荐理由"
                                                                   FontSize="{StaticResource SmallFontSize}"
                                                                   FontWeight="Bold"
                                                                   Foreground="{StaticResource WarningAccent}"/>
                                                        <TextBlock Text="{Binding Reason}"
                                                                   FontSize="{StaticResource BodyFontSize}"
                                                                   TextWrapping="Wrap"
                                                                   LineHeight="{StaticResource CompactLineHeight}"
                                                                   Foreground="{StaticResource WarningTextDark}"/>
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                            
                                            <!-- 第五部分：分隔线 -->
                                            <Border Height="{StaticResource DividerHeight}"
                                                    Background="{DynamicResource DividerBrush}"/>
                                            
                                            <!-- 第六部分：操作按钮 -->
                                            <StackPanel Orientation="Horizontal" 
                                                        Spacing="{StaticResource SmallSpacing}">
                                                <Button Content="查看详情" 
                                                        Command="{Binding #Root.DataContext.ViewStockDetailCommand}"
                                                        CommandParameter="{Binding}"
                                                        Padding="{StaticResource DefaultCardPadding}"
                                                        FontSize="{StaticResource SmallFontSize}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"/>
                                                <Button Content="加入自选" 
                                                        Command="{Binding #Root.DataContext.AddToFavoritesCommand}"
                                                        CommandParameter="{Binding}"
                                                        Padding="{StaticResource DefaultCardPadding}"
                                                        FontSize="{StaticResource SmallFontSize}"
                                                        CornerRadius="{StaticResource SmallCornerRadius}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    
                    <!-- 风险提示 -->
                    <Border Background="{StaticResource WarningBackgroundAlt}"
                            BorderBrush="{StaticResource WarningBorderAlt}"
                            BorderThickness="{StaticResource ThickBorderThickness}"
                            CornerRadius="{StaticResource CardCornerRadius}"
                            Padding="{StaticResource CardPadding}"
                            Margin="{StaticResource SmallTopMargin}"
                            IsVisible="{Binding HasRiskWarnings}">
                        <StackPanel Spacing="{StaticResource DefaultSpacing}">
                            <TextBlock Text="⚠️ 风险提示" 
                                       FontSize="{StaticResource SubTitleFontSize}" 
                                       FontWeight="Bold"
                                       Foreground="{StaticResource WarningAccent}"/>
                            <TextBlock Text="{Binding FormattedRiskWarnings}"
                                       FontSize="{StaticResource LabelFontSize}"
                                       Foreground="{StaticResource WarningText}"
                                       TextWrapping="Wrap"
                                       LineHeight="{StaticResource DefaultLineHeight}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </controls:CardView>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
